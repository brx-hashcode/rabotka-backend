# Generated by generate.ts / create.ts. fixtures.json is committed on purpose —
# the translation tests run against it, and a test whose input is not in the
# repo silently skips in CI.
payloads/
report.md
created.json
media-handle.json
